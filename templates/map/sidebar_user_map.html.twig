<div id="sidebar-trigger-zone" class="trigger-tab">
    <span class="arrow-icon">◀</span>
</div>

<div id="sidebar-panel" class="sidebar-container">

    <div class="sidebar-main-box">

        <div class="sidebar-content">

            {% if is_granted('ROLE_ACCOMPANYING') %}

                <div class="info-block">
                    <span class="label">MON GROUPE</span>
                    <span class="value">{{ currentUser.groupCode ?? '-' }}</span>
                </div>

                {# Grille élèves — remplie par JS (loadGroup) #}
                <div id="group-list" class="group-grid">
                    <p class="group-loading">Chargement..</p>
                </div>

               <!-- Partie dashboard -->
                <div class="info-block text-center mt-3">
                    <a href="{{ path('app_accompanying_dashboard') }}" class="btn btn-primary btn-sm w-100">
                        📊 Voir le Bilan Complet
                    </a>
                </div>

            {% elseif currentUser and userStats %}

                <div class="info-block">
                    <span class="value">{{ currentUser.pseudo }}</span>
                </div>

                <div class="avatar-block">
                    <span class="avatar avatar--md">
                        <img src="{{ asset('images/coccitest.webp') }}"
                             alt="Avatar de {{ currentUser.pseudo }}">
                    </span>
                </div>

                <div class="info-block school-block">
                    <span class="value">{{ currentUser.group?.establishment?.name|default('ÉTABLISSEMENT') }}</span>
                    <span class="value">{{ currentUser.group?.name|default('CLASSE') }}</span>
                </div>

                <div class="info-block score-block">
                    <span class="label">SCORE PERSO:</span>
                    <span class="value">{{ userStats.totalScore|default(0) }}</span>
                </div>

                <div class="info-block score-block">
                    <span class="label">SCORE GROUPE:</span>
                    <span class="value">{{ userStats.totalScoreGroup|default(0) }}</span>
                </div>

            {% else %}
                <div class="info-block">
                    <p>Données indisponibles.</p>
                </div>
            {% endif %}

            <div class="footer-block">
                <a href="#" class="mentions-link">MENTIONS LEGALES</a>
            </div>

        </div>
    </div>
</div>
